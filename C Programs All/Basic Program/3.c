#include<stdio.h>
int main(){
    

    //Area of Rectangle Program

    int rec,len,width;
    printf("Enter the value of length and width");
    scanf("%d%d",&len,&width);
    rec=len*width;
    printf("The rectangle value is =%d",rec);
    
    return 0;
}